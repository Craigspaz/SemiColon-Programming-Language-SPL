for(var x = 0, x < 2, x = x + 1);
	Print(x);
	for(var y = 0, y < 2, y = y + 1);
		Print("Y is superior");
		for(var z = 0, z < 2, z = z + 1);
			Print("z is superior");
		endloop;
	endloop;
endloop;